# ADGENCOV selection bench-off — results index

Latest: `results/latest.txt`. Each run also archived by timestamp+sha.

Read from any device with git+SSH:
```
git fetch origin bench-results && git show origin/bench-results:results/latest.txt
```
